# TOPPEUR — 功能需求说明书

> 用途:本文件供下一个智能体生成 Claude Code 提示词使用。尽量完整、具体,减少生成提示词时的猜测。

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 产品名 | TOPPEUR |
| 平台 | Windows 10 / Windows 11(x64) |
| 交付形态 | **单文件绿色 EXE**,免安装,下载即用,后台常驻 |
| 一句话定位 | 窗口层级管理工具:右键任意窗口即可调整其 Z 序层级,并以窗口边缘光晕直观显示层级高低 |
| 目标用户 | 普通桌面用户,界面简洁,零配置即可用 |

**核心使用流程:**
1. 启动 EXE → 程序进入系统托盘(不弹主窗口,后台运行)。
2. 用户在某窗口上右键(或按约定修饰键+右键)→ 弹出 TOPPEUR 层级菜单 → 选择"置于第 N 层"。
3. 该窗口边缘立即出现一圈光晕;层级越高,光晕颜色越深。
4. 托盘图标(单击)→ 打开设置界面:光晕颜色、层级数、快捷键、开机自启等。
5. 用户在设置中可录制全局快捷键(如 Ctrl+Alt+T)一键置顶当前窗口。

---

## 2. 核心功能需求

### 2.1 右键任意窗口 → 选择层级

- **目标:** 在任意第三方应用窗口上右键时,能调出 TOPPEUR 的层级选择菜单,菜单作用于光标下的窗口。
- **技术现实(必须向生成提示词的智能体说明):** 无法把自定义菜单注入第三方程序自己的右键菜单。可行方案是 **全局低级鼠标钩子 `SetWindowsHookEx(WH_MOUSE_LL)`**:检测到右键按下 → 用 `WindowFromPoint` 取得光标下的窗口 → 弹出 TOPPEUR 自己的弹出菜单。
- **防误触(重要 UX 决策):** 若裸右键就弹菜单,会破坏所有应用的正常右键菜单,不可接受。**默认方案:按住 `Ctrl` + 右键** 弹出 TOPPEUR 菜单(推荐);备选:长按右键 ≥500ms。触发方式在设置中可切换(Ctrl+右键 / 长按右键 / Alt+右键),并允许整体禁用。
- **窗口识别:**
  - 用 `GetAncestor(hwnd, GA_ROOT)` 取根窗口,避免取到子控件。
  - 过滤:桌面窗口、任务栏、TOPPEUR 自身窗口。
  - UWP 应用窗口层级调整可能受限(ApplicationFrameHost),菜单中该项灰显或提示。
- **权限:** 目标窗口若以管理员权限运行,非提权的 TOPPEUR 无法修改其 Z 序 → 检测失败时在菜单/气泡中提示"需以管理员身份运行 TOPPEUR"。

### 2.2 层级定义与 Z 序调整

- **层级:** 离散层级 1~N,数字越大越靠前(越接近顶层)。**默认 N=3:**
  - 层级 1 = 置底(落入 `HWND_BOTTOM` 所在带)
  - 层级 2 = 普通(`HWND_NOTOPMOST` 带内正常位置)
  - 层级 3 = 置顶(`HWND_TOPMOST` 带)
- 层级数 N 可在设置中配置(如 3~9),更多层级通过在同带内多次调整相对顺序近似实现;实现时明确说明 Windows Z 序只有 topmost/普通/bottom 三个带,同一带内的相对次序由 `SetWindowPos` 插入位置决定。
- **实现 API:** `SetWindowPos(hwnd, HWND_TOPMOST | HWND_NOTOPMOST | HWND_BOTTOM, ..., SWP_NOMOVE | SWP_NOSIZE | SWP_NOACTIVATE)`,避免激活/移动目标窗口。
- **置顶切换:** 对已置顶窗口再次选择层级 2(普通)即取消置顶。

### 2.3 边缘光晕(层级可视化)

- **效果:** 窗口被设置层级后,其边缘出现一圈光晕;**层级越高,光晕颜色越深**。光晕持续显示,直到该窗口层级被改回普通/取消、或窗口关闭。
- **实现方案(推荐):外描边覆盖窗口**
  - 为每个被设层级的窗口创建一个比目标窗口四周各大 N 像素(如 8px)的无边框覆盖窗口,绘制光晕(高斯渐变描边)。
  - 覆盖窗口样式:`WS_EX_LAYERED | WS_EX_TRANSPARENT`(鼠标穿透)| `WS_EX_NOACTIVATE` | `WS_EX_TOOLWINDOW`(不占任务栏/Alt-Tab),用 `UpdateLayeredWindow`(per-pixel alpha)或 Direct2D 绘制。
  - 光晕窗口自身置为 topmost,且始终与目标窗口保持同步。
- **备选方案:** 内描边(在窗口内侧边缘画线),用不规则区域 `SetWindowRgn` 或覆盖窗口 + 空心区域实现。若外描边在多显示器下问题多,可切换为内描边。
- **跟随机制:** `SetWinEventHook` 监听目标窗口的 `EVENT_OBJECT_LOCATIONCHANGE`、`EVENT_SYSTEM_MOVESIZEEND`、`EVENT_OBJECT_DESTROY`、`EVENT_OBJECT_HIDE/SHOW`、`EVENT_SYSTEM_MINIMIZESTART/END`,实时移动/缩放/隐藏光晕窗口;另加低频轮询(如 200ms)兜底。
- **颜色深度映射:** 层级 1 → 光晕约 25% 不透明度,层级 N(顶层)→ 约 90%,中间线性插值;映射表可配置。
- **多窗口:** 维护 `hwnd → overlay` 字典,支持多个窗口同时带光晕;窗口销毁时务必清理,防止泄漏。
- **DPI:** 进程设为 Per-Monitor V2 DPI Aware,避免多显示器/缩放错位。

### 2.4 光晕颜色自定义

- 设置界面提供**颜色选择器**(标准取色盘 + 常用色预设),指定光晕的**基准色**(默认建议青色或金色,视觉醒目)。
- 深浅由层级决定(同 2.3 的透明度/亮度映射),用户只选基准色即可。
- 即时预览:设置界面里放一个示例色块条,展示 1~N 层对应的深浅效果。

### 2.5 全局自定义快捷键

- **默认快捷键:** `Ctrl+Alt+T` = 将当前前台窗口(或光标下窗口)置顶;**再次按下 = 取消置顶**(toggle)。
- **可自定义动作(至少支持):**
  - 置顶/取消置顶(当前窗口)
  - 置底 / 恢复普通层级
  - 层级 +1 / 层级 -1
  - 弹出层级选择菜单(在光标处)
- **录制式快捷键编辑器:** 点击输入框 → 按下组合键 → 自动记录显示;保存时做冲突检测(与已注册热键冲突时提示)。
- **实现 API:** `RegisterHotKey` / `UnregisterHotKey`;注册失败(被其他程序占用)要给出提示而非静默失败。

### 2.6 开机自启

- 设置界面提供"开机自动启动"开关。
- **实现:** 写入注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`(无需管理员);关闭开关时删除该键。
- 可选加分项:程序首次运行询问一次"是否开机自启?"(气泡提示,不打断)。

### 2.7 后台运行 + 托盘 + 设置界面

- **启动行为:** 程序启动即最小化到系统托盘(`Shell_NotifyIcon`),不抢占焦点;常驻后台。
- **托盘交互:** 单击托盘图标(或双击,取其一,设置中可改)→ 打开设置界面;"点开始,进入选项界面"即此路径。
- **托盘右键菜单:** 打开设置 / 暂停捕获(临时禁用右键菜单与光晕)/ 开机自启(勾选项)/ 退出。
- **单实例:** 用命名 Mutex 保证只运行一个实例;重复启动时唤起已有实例的设置窗口。
- **配置持久化:** `%APPDATA%\TOPPEUR\settings.json`(颜色、层级数、快捷键、触发方式、自启状态等),启动时读取,修改即保存。
- **界面语言:** 中文(后续可扩展 i18n,但不作为首期需求)。

---

## 3. 设置界面(UI)设计建议

单窗口,左侧标签页或上下分组:

1. **常规:** 开机自启开关、暂停捕获开关、右键触发方式(Ctrl+右键/长按右键/Alt+右键/禁用)、层级数量 N。
2. **光晕:** 颜色选择器、层级深浅预览条、光晕宽度(px)、光晕样式(外描边/内描边)。
3. **快捷键:** 动作列表 + 录制式输入框 + 冲突提示。
4. **关于:** 版本号、开机自启状态说明、提权提示。

视觉风格:简洁现代,暗色/亮色跟随系统即可;不引入重型 UI 框架。

---

## 4. 技术选型(供决策,附推荐)

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **C# .NET 8(WinForms)+ P/Invoke,发布 self-contained 单文件** | 开发快、Claude Code 生成质量稳定、单文件发布简单、P/Invoke 足够覆盖全部所需 API | 默认体积约 70MB(可用 PublishTrimmed 减小;NativeAOT 在 .NET 8 对 WinForms 支持不成熟) | **推荐** |
| C++(Win32) | 体积 <1MB、启动快、无运行时 | 开发慢、内存管理易错 | 若极度在意体积可选 |
| Rust(windows-rs) | 体积适中、内存安全 | 编译链复杂、开发速度一般 | 备选 |
| Python + PyInstaller | 开发最快 | 体积大、启动慢、杀软误报多、常驻内存高 | 不推荐 |
| Electron / Tauri | 跨平台 | 体积大、常驻内存高,不适合常驻托盘小工具 | 不推荐 |

> 建议在提示词中明确:"使用 C# .NET 8 WinForms,P/Invoke 调用 Win32 API,发布命令 `dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:PublishTrimmed=true`"。

---

## 5. Win32 关键技术点清单(提示词中应要求实现)

| 功能 | API |
|---|---|
| Z 序调整 | `SetWindowPos`,`HWND_TOPMOST` / `HWND_NOTOPMOST` / `HWND_BOTTOM`,`SWP_NOACTIVATE` |
| 光标下窗口 | `WindowFromPoint`,`GetAncestor(GA_ROOT)` |
| 窗口可见性过滤 | `DwmGetWindowAttribute(DWMWA_CLOAKED)`,`IsWindowVisible` |
| 右键捕获 | `SetWindowsHookEx(WH_MOUSE_LL)` |
| 全局热键 | `RegisterHotKey` / `UnregisterHotKey` |
| 光晕覆盖窗口 | `WS_EX_LAYERED` + `UpdateLayeredWindow`(或 Direct2D) |
| 光晕跟随 | `SetWinEventHook`(`EVENT_OBJECT_LOCATIONCHANGE` 等)+ 轮询兜底 |
| 托盘 | `Shell_NotifyIcon` |
| 开机自启 | 注册表 `HKCU\...\CurrentVersion\Run` |
| DPI | `SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2)` |
| 单实例 | `Mutex` |

---

## 6. 边界情况与坑(要求实现时显式处理)

- UWP 窗口(ApplicationFrameHost)层级调整受限 → 灰显/提示。
- 管理员权限窗口无法被非提权进程操作 → 提示提权。
- 全屏/独占全屏(游戏)下光晕可能不可见 → 接受此限制,勿崩溃。
- 多显示器、DPI 缩放 → per-monitor v2,坐标用物理像素换算。
- 窗口移动/缩放/最小化/关闭/虚拟桌面切换 → 光晕实时跟随,关闭即销毁,防泄漏。
- 热键冲突 → 注册失败提示,不静默。
- 任务栏/桌面/自身窗口 → 过滤,不出菜单。
- 杀软对全局钩子可能误报 → 文档说明;有条件可做代码签名(见第 7 节)。

---

## 7. 发布与分发

- 单文件 exe(发布命令见第 4 节),绿色免安装。
- 可选:代码签名证书(减少 SmartScreen/杀软拦截)。
- 版本号格式 `x.y.z`;托盘"关于"中显示。

---

## 8. 验收标准(提示词中应作为"完成后必须验证"清单)

1. 在干净的 Win11(无任何运行时安装)上双击 exe 可直接运行,进入托盘。
2. Ctrl+右键任意第三方窗口(浏览器/记事本/文件管理器等)→ 弹出 TOPPEUR 菜单。
3. 选择"层级 3(置顶)"→ 目标窗口置顶且边缘出现光晕;层级越高光晕越深。
4. 拖动/缩放/最小化/关闭目标窗口 → 光晕正确跟随/隐藏/消失,程序不崩溃、无内存泄漏(长跑 1 小时)。
5. 设置中改光晕颜色 → 所有光晕即时变色。
6. 录制自定义快捷键(如 Ctrl+Alt+T)→ 一键置顶当前窗口,再按取消;重启程序后快捷键仍生效。
7. 打开"开机自启"→ 注册表出现对应 Run 键;重启电脑后程序自动进托盘;关闭开关后 Run 键消失。
8. 重复启动 exe → 不产生第二个实例,而是唤起已有实例。
9. 托盘:单击打开设置;右键菜单各项可用;退出彻底结束进程。

---

## 9. 待确认的决策点(生成提示词时需用户拍板或按推荐默认)

| # | 问题 | 推荐默认 |
|---|---|---|
| 1 | 技术栈 | C# .NET 8 WinForms 单文件发布 |
| 2 | 右键触发方式 | Ctrl+右键(可配置) |
| 3 | 层级数 | 3 级(置底/普通/置顶),可配置 3~9 |
| 4 | 光晕形态 | 外描边(8px),持续显示;备选内描边 |
| 5 | 界面语言 | 中文 |
| 6 | 是否需要"已管理窗口列表"面板 | 首期不做,托盘菜单可加"清除全部光晕" |

---

## 10. 附录:可复用的 Claude Code 提示词草稿

> 下一智能体可直接润色后使用:

```
请开发一个 Windows 窗口层级管理工具 TOPPEUR,最终交付为可直接运行的
win-x64 单文件 exe(免安装、绿色版)。

【技术栈】C# .NET 8 + WinForms,P/Invoke 调用 Win32 API。
发布方式:dotnet publish -c Release -r win-x64 --self-contained true
-p:PublishSingleFile=true -p:PublishTrimmed=true

【功能需求】
1. 后台常驻 + 托盘:程序启动即最小化到系统托盘(Shell_NotifyIcon),不弹主窗口;
   单击托盘图标打开设置界面;托盘右键菜单含:打开设置 / 暂停捕获 / 开机自启(勾选) / 退出;
   Mutex 保证单实例,重复启动唤起已有实例;配置存 %APPDATA%\TOPPEUR\settings.json。
2. 右键任意窗口选择层级:用 WH_MOUSE_LL 全局钩子,当用户按住 Ctrl 并在任意窗口上
   点击右键时,弹出 TOPPEUR 层级菜单(作用于 WindowFromPoint + GA_ROOT 取得的窗口,
   过滤桌面/任务栏/自身窗口);菜单提供层级 1(置底)/ 2(普通)/ 3(置顶),
   通过 SetWindowPos(HWND_BOTTOM / HWND_NOTOPMOST / HWND_TOPMOST, SWP_NOACTIVATE) 调整;
   层级数 N 在设置中可配置(3~9)。
3. 边缘光晕:为被设层级的窗口创建比其四周大 8px 的鼠标穿透无边框覆盖窗口
   (WS_EX_LAYERED | WS_EX_TRANSPARENT | WS_EX_NOACTIVATE | WS_EX_TOOLWINDOW),
   用 UpdateLayeredWindow 绘制渐变描边光晕;层级越高光晕颜色越深
   (如 25% → 90% 不透明度线性映射);基准颜色在设置中可用颜色选择器自定义;
   用 SetWinEventHook 监听窗口移动/缩放/最小化/关闭,光晕实时跟随,窗口关闭即销毁;
   支持多个窗口同时带光晕;进程设为 Per-Monitor V2 DPI Aware。
4. 全局快捷键:设置中录制式编辑组合键,默认 Ctrl+Alt+T = 切换当前前台窗口置顶/取消置顶;
   另支持:置底、层级+1/-1、光标处弹出层级菜单;RegisterHotKey 注册失败需提示冲突。
5. 开机自启:设置开关,写入/删除注册表 HKCU\Software\Microsoft\Windows\CurrentVersion\Run。
6. 边界处理:UWP/管理员权限窗口受限时给出提示;目标窗口关闭时清理所有资源防泄漏;
   全屏独占下光晕不可见可接受但不得崩溃。

【代码要求】按职责分模块(钩子/层级管理/光晕管理/热键/托盘/设置/配置存储),
关键 Win32 交互处写注释;设置界面简洁,中文字符串,即时预览光晕颜色深浅。

【验收标准】在干净 Win11 上双击 exe 即用;Ctrl+右键弹菜单、置顶生效、光晕跟随窗口
移动缩放、改颜色即时生效、自定义快捷键重启后仍生效、开机自启注册/注销正确、
重复启动唤起已有实例、长跑 1 小时无崩溃无内存泄漏。
```

---

*文档版本:v1.0(2026-09-12)*
